spring:
  application:
    name: service3
  config:
    import: classpath:datadog.yml, classpath:logging.yml, classpath:tracing.yml
server:
  port: 8083

management:
  metrics:
    export:
      statsd:
        enabled: true
        host: datadog-agent
        port: 8125
        flavor: datadog